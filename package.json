{
  "name": "peglab",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Kaspa Testnet-10 classroom: tiny pool will depeg. Receipt is 1 sompi. Not USD.",
  "license": "MIT",
  "scripts": {
    "test": "node --test tests/peglab.test.mjs tests/receipt.test.mjs tests/host-wallet.test.mjs",
    "fixtures": "node server/peglab.mjs --fixtures",
    "compile": "node server/peglab.mjs --compile",
    "genesis": "node server/submit-genesis.mjs",
    "genesis:submit": "node server/submit-genesis.mjs --submit",
    "serve": "node server/serve.mjs",
    "pages": "node scripts/build-pages.mjs"
  },
  "engines": {
    "node": ">=20"
  }
}
